fix the cargo.toml
add vector for messages
revert display to take no args
add DispMessage option to lights
add lots of DispMessage calls which explain what's happening to the code
Change the code to use a singular enum instead of having both TrafficLightState and PedestrianLightState
  - Green
  - GreenToRed
  - RedCrossPending
  - RedCross
  - RedCrossEnding
  - RedToGreen
Add a load of comments
Ensure all the variables are suitable
Make the ReadMe nicer